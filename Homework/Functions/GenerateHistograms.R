# --------------------------------------
# FUNCTION generate_histograms
# required packages: none
# description:
# inputs:
# outputs:
########################################
generate_histograms <- function(x=NULL,y=NULL){

# assign parameter defaults
if (is.null(x) | is.null(y)) {
  x <- runif(10)
  y <- runif(10)
}

# function body



return(print('...checking function: generate_histograms()'))

} # end of function generate_histograms
# --------------------------------------
# generate_histograms()
